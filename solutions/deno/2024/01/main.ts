#!/usr/bin/env deno run

import { runner } from "../../../../lib/deno.ts";

function solve_pt1(input_data: string, args?: string[]): number {
	return 0;
}

function solve_pt2(input_data: string, args?: string[]): number {
	return 0;
}

runner(solve_pt1, solve_pt2);
